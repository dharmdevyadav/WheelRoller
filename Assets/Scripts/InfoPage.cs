using System.Collections;
using System.Collections.Generic;
using UnityEditor.Rendering;
using UnityEngine;

public class InfoPage : MonoBehaviour
{
    
    //TimerCounter TimerScript;
    //NewBehaviourScript NewScript;
    void Start()
    {
        //NewScript = FindObjectOfType<NewBehaviourScript>();
       // TimerScript =FindObjectOfType<TimerCounter>();
        
    }

    
    void Update()
    {
        /*if(TimerScript.currentTimer == 0 && NewScript.RayCastObj == true && NewScript.btnCancel.interactable == false)
        {
            PopupWindow.SetActive(true);
        }
        else
        {
            PopupWindow.SetActive(false);
        }*/
    }
}
